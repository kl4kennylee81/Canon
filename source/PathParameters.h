//All in world coordinates
#define MIN_DISTANCE 20.0 //per frame, must be greater than velocity ?
#define AI_SPEED 1.0 //per frame
#define VELOCITY 14.0 //per frame
#define RADIUS 9.0 //(radius * 2) > velocity + 3 ? Basically it needs to be slightly more than half the velocity
#define TOUCH_RADIUS 100.0
